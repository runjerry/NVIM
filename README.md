# NVIM
My personal neovim configurations. Some dependencies:
- coc.nvim depends on nodejs
- search in fzf uses ripgrep, which is built on top of rust
- fzf layout colorscheme depends on bat (set BAT_THEME env variable)
